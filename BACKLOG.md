# Backlog

1. [x] Scaffolding & health endpoint — create .NET 10 minimal API project (`dotnet new web`), register `GET /health` returning `{ "status": "healthy" }` with HTTP 200, configure xUnit test project with a passing placeholder test <!-- depends: none -->
